function capitalizeString(string) {
  return string.toUpperCase()
}

const result1 = capitalizeString("hello")

function multiplyNums(num1, num2) {
  return num1 * num2;
}

const result2 = multiplyNums(2, 3);